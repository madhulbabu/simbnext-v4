---
import BaseLayout from '@/layouts/BaseLayout.astro';
import { DESKS } from '@/lib/content';

export function getStaticPaths() {
  return DESKS.map((desk) => ({
    params: { slug: desk.slug },
    props: { desk },
  }));
}

const { desk } = Astro.props;
---

<BaseLayout
  title={`${desk.name} — ${desk.category}`}
  description={desk.description}
>
  <!-- Light Header Section -->
  <div class="pt-36 pb-20 bg-gradient-to-b from-[#EFF6FF] via-[#F8FAFC] to-white text-[#0F172A] border-b border-[#E2E8F0]">
    <div class="container-wide">
      <div class="max-w-3xl space-y-4">
        <div class="flex items-center gap-3">
          <span class="chip-synthesia">{desk.category}</span>
          <span class="text-xs font-mono text-[#64748B]">Desk {desk.index} of 06</span>
        </div>
        <h1 class="headline-lg text-[#0F172A] [text-wrap:balance]">
          {desk.name}
        </h1>
        <p class="text-lg text-[#2563EB] font-semibold">
          {desk.tagline}
        </p>
        <p class="body-md text-[#334155] leading-relaxed [text-wrap:balance]">
          {desk.description}
        </p>

        <div class="pt-4 flex items-center gap-4">
          <a href="/briefing" class="button-primary">
            <span>Schedule a Briefing for {desk.name}</span>
            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M5 12h14"/><path d="m12 5 7 7-7 7"/>
            </svg>
          </a>
        </div>
      </div>
    </div>
  </div>

  <!-- Content Section -->
  <div class="py-20 bg-[#FFFFFF]">
    <div class="container-wide">
      <div class="grid gap-14 lg:grid-cols-[1.2fr_0.8fr] items-start">
        
        <!-- Left Column: Deliverables & Outcomes -->
        <div class="space-y-10">
          <!-- SLA Guarantee Box (Ice Blue Tint) -->
          <div class="rounded-2xl bg-[#EFF6FF] p-6 border border-[#DBEAFE]">
            <div class="flex items-center gap-2 text-xs font-mono font-bold text-[#2563EB] uppercase tracking-wider mb-2">
              <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/>
              </svg>
              Service Level Agreement (SLA) Commitment
            </div>
            <p class="text-base font-semibold text-[#0F172A]">
              {desk.sla}
            </p>
          </div>

          <!-- Included Deliverables -->
          <div>
            <h2 class="headline-md text-[#0F172A] mb-5">
              Included Monthly Deliverables & Capacity
            </h2>
            <div class="grid gap-3">
              {desk.deliverables.map((item) => (
                <div class="p-5 rounded-2xl border border-[#E2E8F0] bg-[#F8FAFC] flex items-start gap-3">
                  <span class="h-2 w-2 rounded-full bg-[#2563EB] mt-2 shrink-0"></span>
                  <span class="text-sm font-medium text-[#0F172A]">{item}</span>
                </div>
              ))}
            </div>
          </div>

          <!-- Business Outcomes -->
          <div>
            <h2 class="headline-md text-[#0F172A] mb-5">
              Business Outcomes & Impact
            </h2>
            <div class="p-6 rounded-2xl border border-[#E2E8F0] bg-white space-y-3 shadow-xs">
              {desk.outcomes.map((outcome) => (
                <div class="flex items-start gap-3 text-sm text-[#334155]">
                  <span class="text-[#2563EB] font-bold">✓</span>
                  <span>{outcome}</span>
                </div>
              ))}
            </div>
          </div>
        </div>

        <!-- Right Column: Desk Explorer Sidebar -->
        <div class="space-y-6 lg:sticky lg:top-28">
          <div class="card-synthesia p-6">
            <h3 class="headline-sm text-[#0F172A] mb-4">
              The 6 Desks Architecture
            </h3>
            <div class="space-y-2">
              {DESKS.map((d) => (
                <a
                  href={`/services/${d.slug}`}
                  class:list={[
                    "p-3 rounded-xl flex items-center justify-between text-xs font-semibold transition-colors",
                    d.slug === desk.slug
                      ? "bg-[#EFF6FF] text-[#2563EB] border border-[#DBEAFE]"
                      : "text-[#334155] hover:bg-[#F8FAFC]"
                  ]}
                >
                  <span>{d.name}</span>
                  <span class="text-[10px] font-mono text-[#64748B]">{d.index}</span>
                </a>
              ))}
            </div>
          </div>

          <div class="rounded-3xl bg-[#EFF6FF] border border-[#DBEAFE] p-6 space-y-4">
            <h4 class="text-base font-bold text-[#0F172A]">Ready to activate {desk.name}?</h4>
            <p class="text-xs text-[#334155] leading-relaxed">
              Book a discovery consultation. We will analyze your operations and provide a tailored SLA proposal.
            </p>
            <a href="/contact" class="button-primary w-full text-center text-xs">
              <span>Schedule a Consultation</span>
            </a>
          </div>
        </div>

      </div>
    </div>
  </div>
</BaseLayout>
