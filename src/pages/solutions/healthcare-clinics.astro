---
import BaseLayout from '@/layouts/BaseLayout.astro';
import { SOLUTIONS } from '@/lib/content';

const solution = SOLUTIONS.find((s) => s.slug === 'healthcare-clinics')!;
---

<BaseLayout
  title="Digital Operations for Healthcare & Aesthetic Clinics | SimbNext"
  description="24/7 AI patient triage, automated appointment booking, anti-no-show reminders, and local SEO for dental, dermatology, and aesthetic clinics."
>
  <!-- Light Header Section -->
  <div class="pt-36 pb-20 bg-gradient-to-b from-[#EFF6FF] via-[#F8FAFC] to-white text-[#0F172A] border-b border-[#E2E8F0]">
    <div class="container-wide max-w-3xl space-y-4">
      <span class="chip-synthesia">
        Industry Solution
      </span>
      <h1 class="headline-lg text-[#0F172A] [text-wrap:balance]">
        {solution.title}
      </h1>
      <p class="body-lg text-[#334155] [text-wrap:balance]">
        {solution.description}
      </p>
      <div class="pt-2">
        <a href="/contact" class="button-primary">
          <span>Schedule a Consultation</span>
          <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M5 12h14"/><path d="m12 5 7 7-7 7"/>
          </svg>
        </a>
      </div>
    </div>
  </div>

  <div class="py-20 bg-[#FFFFFF]">
    <div class="container-wide max-w-4xl space-y-12">
      <!-- Metric Banner -->
      <div class="p-8 rounded-3xl bg-[#EFF6FF] border border-[#DBEAFE] flex flex-col sm:flex-row items-center justify-between gap-6 shadow-xs">
        <div>
          <div class="text-3xl sm:text-4xl font-extrabold font-mono text-[#2563EB]">
            {solution.stat}
          </div>
          <div class="text-sm font-semibold text-[#0F172A] mt-1">
            Average Lift in Patient Consultation Bookings
          </div>
        </div>
        <div class="text-xs text-[#64748B] max-w-xs leading-relaxed">
          Based on verified deployment of SimbNext 24/7 AI Lead Triage and Google Maps 3-Pack optimization.
        </div>
      </div>

      <!-- Core Capabilities -->
      <div class="space-y-6">
        <h2 class="headline-md text-[#0F172A]">
          Specialized Clinic Capabilities Included
        </h2>
        <div class="grid gap-4 sm:grid-cols-2">
          {solution.deliverables.map((d) => (
            <div class="card-synthesia p-6">
              <div class="text-[#2563EB] font-bold text-lg mb-2">✓</div>
              <p class="text-sm font-medium text-[#0F172A] leading-relaxed">
                {d}
              </p>
            </div>
          ))}
        </div>
      </div>

      <!-- Testimonial Quote -->
      <div class="p-8 rounded-3xl bg-[#F8FAFC] border border-[#E2E8F0] space-y-4 shadow-xs">
        <div class="text-amber-500 text-sm font-semibold">★★★★★ Verified Clinic Partner</div>
        <p class="text-base text-[#0F172A] italic leading-relaxed font-medium">
          "Our 24/7 AI triage agent captures 3x more patient consultations after clinic hours with zero additional staff overhead."
        </p>
        <div class="text-xs text-[#64748B] font-mono pt-3 border-t border-[#E2E8F0]">
          Dr. Radhika V. • Clinical Director & Founder • Apex Health Clinics
        </div>
      </div>

      <!-- Bottom CTA -->
      <div class="text-center pt-6">
        <a href="/contact" class="button-primary">
          <span>Schedule a Consultation</span>
        </a>
      </div>
    </div>
  </div>
</BaseLayout>
